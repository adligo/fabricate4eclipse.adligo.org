This will eventually become a eclipse plugin.

However the first setp is a fabricate command plugin which 
copies the classpath from fabricate project.xml, fabricate.xml libs
to the eclipse .classpath files.